```css
// Margin can do negative syntax


margin: 10px; // applies to all 4
margin: 10px 5px; // top bottom; left right;
margin: 10px 5px 8px; // top; left right; bottom
margin: 10px 5px 8px 6px; top; right; bottom; left;


// Padding behaves similiarly but can't do negative values
```