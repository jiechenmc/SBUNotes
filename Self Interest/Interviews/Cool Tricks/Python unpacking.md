```python
# Python unpacking demo

a = [1, 2, 3]

# The following is the same as
# print(1, 2, 3)

print(*a)

# * will unpack every element of the iterable and pass it in to the print function as arguments
```